apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-appv4
spec:
  replicas: 5
  selector:
    matchLabels:
      app: flask
  template:
    metadata:
      labels:
        app: flask
    spec:
      containers:
      - name: flask
        image: ismail17/flask-app:v4
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
